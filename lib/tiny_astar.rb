require 'time'

class TinyAstar
  # map: a 2d array representing the map with passable and impassable nodes
  # start: the coordinates (x, y) of the starting node
  # finish: the coordinates (x, y) of the finishing node
  def initialize(map:, start:, finish:)
  end
end
